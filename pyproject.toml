[project]
name = "batch-spark"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "jude-craft", email = "derekjude254@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "jupyter>=1.1.1",
    "pandas<3.0.0",
    "pyarrow>=25.0.1",
    "pyspark>=4.2.0",
]

[project.scripts]
batch-spark = "batch_spark:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
